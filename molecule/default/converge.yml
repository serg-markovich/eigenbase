---
- name: Converge
  hosts: all
  gather_facts: true
  tasks:
    - name: Include role
      ansible.builtin.include_role:
        name: "{{ lookup('env', 'MOLECULE_ROLE_NAME') | default('base') }}"
